BDJoin

## Iniciar programa

El proyecto tiene varias librerias que descaregar para que todo funcione de forma correcta.

Ejecutar comando en terminal `npm install`

Para ejecutar el proyecto `npm run dev`

## Modo Mock

El proyecto usa datos simulados (mock) para desarrollo sin backend.

Para cambiar a la API real, edita `src/api/auth/index.ts`:
